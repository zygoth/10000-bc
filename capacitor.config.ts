import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.fishtfightstarter',
  appName: 'Steam Game Template',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
